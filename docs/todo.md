# TODO: Luca Chinese Chat

今後の開発用TODOです。詳細な調査メモは `docs/codex-review-2026-06-16.md` を参照してください。

## 優先度高

- [ ] iOS Safari向けに `.phone` の `height:100vh` を `100dvh` ベースへ見直す。
- [ ] `.dock` と通話ボタン群に `env(safe-area-inset-bottom)` を追加する。
- [ ] viewportから `maximum-scale=1.0, user-scalable=no` を外す。
- [ ] プレゼント送信時に `save()` を呼び、親密度がリロード後も保持されるようにする。
- [ ] `restartAll()` を完全リセットにするか、読み返し機能として文言を変更する。
- [ ] 会話途中のリロード仕様を決め、必要なら `idx` も保存する。
- [ ] Web Speech APIのvoicesロード、再生終了、エラー時の状態管理を改善する。
- [ ] 巨大Base64画像を外部アセットへ分離する。
- [ ] ストーリーデータ由来の `innerHTML` 連結を減らす。

## 優先度中

- [ ] CSS/JS/会話データをファイル分割する。
- [ ] Dayごとの会話データをJSONまたはJSモジュールに分ける。
- [ ] `DAYS`、`CALL`、`GIFTS` の簡易スキーマチェックを追加する。
- [ ] プレゼント日次キーをUTCではなくローカル日付基準にする。
- [ ] 通話報酬を全期間1回にするか、日次/Day別にするか仕様を決める。
- [ ] 小画面向けに選択肢、ショートカット、入力欄のレイアウトを再設計する。
- [ ] 選択肢内の中国語と日本語訳をモバイルでは2行表示にする。
- [ ] 単語帳・プレゼントパネルに閉じやすい下部ボタンや背景タップを追加する。

## 優先度低

- [ ] `+` ボタンを `div` ではなく `button` にする。
- [ ] パネル開閉時のフォーカス管理を追加する。
- [ ] `aria-expanded`、`aria-controls` などのアクセシビリティ属性を追加する。
- [ ] 吹き出しタップで訳が開くことを初回チップで案内する。
- [ ] レベル計算を固定式ではなくレベルテーブル化する。
- [ ] Playwrightなどで主要画面の表示確認を自動化する。
- [ ] 将来的なAI自由入力に備えて入力欄の仕様を再設計する。

## 改善計画: Web Speech API

- [ ] 音声機能の対応状況を起動時に判定し、非対応ブラウザでは読み上げボタンを無効化して代替メッセージを表示する。
- [ ] `speechSynthesis.getVoices()` の初回空配列に備え、`voiceschanged` を待って中国語音声を再選択する初期化フローを追加する。
- [ ] 中国語音声の選択優先順位を `zh-CN`、`zh-TW`、`zh`、その他の順で明文化し、選択結果をログまたは状態表示で確認できるようにする。
- [ ] 読み上げ開始、終了、キャンセル、エラーを一元管理し、通話ボタンや再生中UIが取り残されないようにする。
- [ ] 連続タップ時は既存の読み上げを `cancel()` してから新しい発話を開始し、多重再生を防ぐ。
- [ ] iOS Safariなど自動再生・ユーザー操作制約が強い環境で、初回タップ後にだけ読み上げを開始する挙動を確認する。
- [ ] エラー時の復旧方針を決め、失敗した発話を再試行するか、テキスト表示のみへフォールバックするかを実装する。
- [ ] 手動確認項目として、Chrome、Safari、iOS Safari、Android Chromeで音声選択・終了イベント・キャンセル動作を検証する。

## 改善計画: Base64画像外部化

- [ ] `index.html` 内のBase64画像を棚卸しし、用途、表示サイズ、再利用有無、想定ファイル名を一覧化する。
- [ ] 外部化先を `assets/` 配下に決め、画像種別ごとに `assets/images/` などの配置ルールを作る。
- [ ] Base64データをPNG/WebPなど適切な形式へ書き出し、画質とファイルサイズを比較して採用形式を決める。
- [ ] HTML/CSS/JS内の参照をData URIから相対パスへ置き換え、既存の表示崩れがないことを確認する。
- [ ] 頻繁に使う画像には幅・高さや代替テキストの指定を追加し、読み込み中のレイアウトシフトを抑える。
- [ ] 初回表示に不要な画像は遅延読み込みまたは必要時読み込みにできるか確認する。
- [ ] 画像外部化後にHTMLサイズ、読み込み回数、表示速度の変化を記録し、キャッシュしやすい構成になっているか確認する。
- [ ] 将来的に画像を差し替えやすいよう、命名規則と追加手順を `docs/` に残す。

## 確認コマンド候補

```bash
git status --short
python3 - <<'PY'
from pathlib import Path
s=Path('index.html').read_text()
start=s.index('<script>')+len('<script>')
end=s.index('</script>')
Path('/tmp/luca-script.js').write_text(s[start:end])
PY
node --check /tmp/luca-script.js
```
